<h1>Editing Post</h1>

<%= render 'edit_form' %>

<%= link_to 'Show', @post %> |
<%= link_to 'Back', ((request.env["HTTP_REFERER"].present? && (request.env["HTTP_REFERER"].to_s.include? "approve")) ? approve_posts_path : url_for(:back))%>
